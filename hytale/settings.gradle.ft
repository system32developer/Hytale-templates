pluginManagement {
    repositories {
        gradlePluginPortal()
        maven {
            url = uri("https://mvn.ultradev.app/snapshots")
        }
    }
}
rootProject.name = '${PROJECT_NAME}'