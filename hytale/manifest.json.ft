{
  "Group": "${PROJECT_NAME}",
  "Name": "${PLUGIN_NAME}",
  "Version": "${BUILD_COORDS.version}",
  "Authors": [
    {
      "Name": "${AUTHORS.toString(", ", "", "")}"
    }
  ],
  "Main": "${MAIN_CLASS}",
  "ServerVersion": "*"
}